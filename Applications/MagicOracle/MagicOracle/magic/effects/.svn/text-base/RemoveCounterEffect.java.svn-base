package magic.effects;

public class RemoveCounterEffect extends Effect{

	/**
	 * It really doesn't matter which of these is called, since we're just 
	 * removing a counter.
	 */
	public void executeEffect() {
		executeEffect(myCard);
	}

	public void executeEffect(Object theObject) {
		myCard.removeCounter();
	}

}
