package magic.effects;

public class UntapTwoLandsEffect extends Effect {

	// It doesn't matter which of these is called.
	
	public void executeEffect() {
		executeEffect(myCard);
	}

	public void executeEffect(Object theObject) {
		myPlayer.getStrategy().untapLand();
		myPlayer.getStrategy().untapLand();
	}

}
