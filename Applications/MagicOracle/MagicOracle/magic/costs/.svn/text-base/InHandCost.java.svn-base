package magic.costs;

import magic.Player;
import cards.Card;

public class InHandCost extends Cost{

	public boolean playable(Player p, Card c) {
		if(!c.isInPlay())
		{
			return true;
		}
		else
		{
			return false;
		}
	}

}
