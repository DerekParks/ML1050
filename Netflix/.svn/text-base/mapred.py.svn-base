#!/usr/bin/env python

  # pipe all training files into mapper.py   ... then pipe the output of mapper into reducer.py

  #bin/hadoop jar contrib/streaming/hadoop-0.16.0-streaming.jar -mapper ./mapper.py -reducer ./reducer.py -input training_set/* -output netflix-output

  # but we don't want the output flag, since reducer creates files for each user..

